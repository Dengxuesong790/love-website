# 🚀 部署指南：后端服务器 + 前端 GitHub Pages

本项目由两部分组成：

| 部分 | 部署位置 | 技术 |
|------|----------|------|
| 前端展示页 + 管理后台页面 | GitHub Pages（自动部署） | 纯 HTML/CSS/JS |
| 后端服务（API + 数据存储） | 服务器 `118.196.43.235` | Python Flask + SQLite |

前端页面（`index.html`、`admin/`）在 GitHub Pages 上，通过 `js/api-config.js` 里配置的地址调用服务器上的后端接口。后端不可用时前端自动降级为内置默认内容，网站仍可正常浏览。

---

## 一、后端部署到服务器（118.196.43.235）

### 1. 准备服务器环境

在服务器上执行（Linux，以 Ubuntu 为例，CentOS 命令大同小异）：

```bash
# 检查 Python 版本（需要 3.8+）
python3 --version

# 如果 Python 版本过低或未安装，先安装：
# Ubuntu/Debian:
sudo apt update && sudo apt install -y python3 python3-venv python3-pip
# CentOS:
# sudo yum install -y python3 python3-pip
```

### 2. 上传后端代码

将本机 `backend/` 目录整个上传到服务器，例如放到 `/opt/love-website/backend/`：

```bash
# 本机执行（或用 WinSCP / FileZilla 等工具上传）
scp -r backend root@118.196.43.235:/opt/love-website/
```

> 注意：上传时**不要**包含 `love_website.db` 和 `uploads/` 目录（如本机已生成），
> 首次启动会自动创建；如已上传请删除，让服务器重新初始化。

### 3. 安装依赖并启动

```bash
cd /opt/love-website/backend

# 创建虚拟环境（推荐，避免污染系统 Python）
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 设置管理员密码（强烈建议修改默认密码 admin123）
export ADMIN_PASSWORD='你的安全密码'

# 首次启动：会自动创建数据库、默认配置和管理员账号
python app.py
```

看到如下输出即启动成功：

```
爱情纪念网站后端服务已启动
API 地址: http://localhost:8080/api
```

### 4. 生产环境：用 gunicorn 长期运行

开发模式 `python app.py` 适合测试，长期运行推荐 gunicorn：

```bash
pip install gunicorn
gunicorn -w 2 -b 0.0.0.0:8080 app:app --daemon
```

### 5. 开机自启（systemd 服务，可选但推荐）

创建服务文件 `/etc/systemd/system/love-backend.service`：

```ini
[Unit]
Description=Love Website Backend
After=network.target

[Service]
User=root
WorkingDirectory=/opt/love-website/backend
Environment=ADMIN_PASSWORD=你的安全密码
ExecStart=/opt/love-website/backend/venv/bin/gunicorn -w 2 -b 0.0.0.0:8080 app:app
Restart=always

[Install]
WantedBy=multi-user.target
```

```bash
sudo systemctl daemon-reload
sudo systemctl enable love-backend
sudo systemctl start love-backend
sudo systemctl status love-backend   # 查看运行状态
```

### 6. 开放防火墙 / 安全组端口

后端监听 **8080** 端口，需要在服务器上放行：

- **云服务器安全组**（阿里云/腾讯云控制台）：入方向放行 TCP 8080
- **服务器防火墙**：
  ```bash
  # Ubuntu
  sudo ufw allow 8080/tcp
  # CentOS
  sudo firewall-cmd --permanent --add-port=8080/tcp && sudo firewall-cmd --reload
  ```

### 7. 验证后端可用

在服务器本机或任意电脑浏览器访问：

```
http://118.196.43.235:8080/api/config/all
```

应返回 JSON（包含站点配置数据），说明后端部署成功。

### 8. 让前端指向服务器

修改项目根目录 `js/api-config.js`：

```js
window.API_BASE = 'http://118.196.43.235:8080/api';
```

（当前文件里已经默认是这个地址。若以后换服务器，只改这一处即可。）

修改后推送 GitHub，前端自动重新部署（见下文）。之后打开网站，配置、情书、相册、音乐都从后端读取。

### 9. 数据备份与迁移

- 数据都在 `backend/love_website.db`（SQLite 数据库）
- 上传的照片/音乐在 `backend/uploads/` 目录
- 备份时把这两个一起拷贝即可；恢复时放回原位置并重启服务

---

## 二、前端 GitHub Pages 自动化部署

项目已内置 GitHub Actions 工作流 `.github/workflows/deploy-pages.yml`。

### 首次配置（只需一次）

1. 将代码推送到 GitHub 仓库 `Dengxuesong790/love-website`
2. 打开仓库 → **Settings** → **Pages**
3. **Build and deployment** → Source 选择 **GitHub Actions**

### 之后每次更新

修改任何前端文件（`index.html`、`css/`、`js/`、`admin/`、`images/`）后推送到 `main` 分支，GitHub Actions 自动构建并发布：

```bash
git add .
git commit -m "更新内容"
git push
```

### 验证部署

- 网站地址：`https://dengxuesong790.github.io/love-website/`
- 管理后台入口：网站右上角"⚙️ 管理后台"按钮，或直接访问 `https://dengxuesong790.github.io/love-website/admin/login.html`
- 首次登录：账号 `admin`，密码为服务器启动时设置的 `ADMIN_PASSWORD`（默认 `admin123`，**请务必修改**）

---

## 三、日常使用速查

| 操作 | 方法 |
|------|------|
| 修改网站标题/昵称/日期/情书/时间线 | 登录管理后台 → 对应 Tab → 保存 |
| 上传/删除相册照片 | 管理后台 → 相册 |
| 更换背景音乐 | 管理后台 → 音乐 |
| 修改后端管理员密码 | 服务器执行 `systemctl restart love-backend`（先改环境变量 ADMIN_PASSWORD） |
| 后端挂了网站还能看吗 | 能，前端自动用内置默认内容 |

---

## 四、常见问题排查

**Q: 网站显示的内容不是后台改的？**
前端加载后端配置需要访问 `http://118.196.43.235:8080`，请确认：后端已启动、8080 端口已放行、`js/api-config.js` 地址正确。浏览器打开 F12 → Console，查看 `✅ 配置加载成功` 还是 `⚠️ 无法从 API 加载配置`。

**Q: 管理后台登录提示"无法连接后端服务"？**
说明浏览器访问不到后端。依次检查：服务器是否运行、端口是否放行、`admin/login.html` 里引用的 `../js/api-config.js` 地址是否正确。

**Q: 上传照片/音乐失败？**
确认文件类型符合要求（图片 jpg/png/gif/webp，音乐 mp3/wav/ogg/m4a），且已登录。

**Q: 改了 GitHub 上的文件但网站没更新？**
检查仓库 Actions 页是否有运行失败；确认部署 Source 已切换为 GitHub Actions。
